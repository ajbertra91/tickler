function Rolodex() {
	var self = this;

	// Calendar is a store of dates.
	self.calendar = {};

  self.currentTime = new Date();
  console.debug('var utcDate = new Date(Date.UTC(96, 11, 1, 0, 0, 0)): ', var utcDate = new Date(Date.UTC(96, 11, 1, 0, 0, 0)););

	return self;
}